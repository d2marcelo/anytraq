package com.brtracker.services.tracking.dao;


import org.hibernate.Criteria;
import org.hibernate.criterion.Restrictions;



// TODO: Auto-generated Javadoc
/**
 * The Class ResultDAOCriteria.
 */
public class ResultDAOCriteria extends AbstractResultDAOCriteria {




/**
 * Sets the job log criteria.
 *
 * @param criteria the new job log criteria
 */
public static void setJobLogCriteria (Criteria criteria) {
		/*
		JobLog jobLog =  (jobLogRequest.getJobLog()== null) ? new JobLog() : jobLogRequest.getJobLog();
		Long id = jobLog.getId();
		Long job_id = jobLog.getJob().getId();
		String userid = jobLog.getJob().getUserid();
		String message = jobLog.getMessage();
		// criterias
		setCommonDAOCriteria(jobLogRequest, criteria);
		if (id != null) criteria.add(Restrictions.eq("id", id));
		if (job_id != null) criteria.add(Restrictions.eq("job.id", job_id));
		if (userid != null){
			criteria.createCriteria("job").add(Restrictions.like("userid","%"+userid+"%"));
		}
		if (message != null) criteria.add(Restrictions.like("message", "%"+message+"%"));
		*/
}


	
}
